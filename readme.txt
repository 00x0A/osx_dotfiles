1. xargs brew install < brew_list.txt
2. echo /usr/local/bin/fish | sudo tee -a /etc/shells
3. chsh -s /usr/local/bin/fish
4. iterm2 > Settings > load settings from custom folder > /Users/[user]/.config/iterm2